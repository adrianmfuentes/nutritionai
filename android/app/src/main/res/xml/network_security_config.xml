<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) traffic for development -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- For emulator -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <!-- For physical device - common local IP ranges -->
        <domain includeSubdomains="true">192.168.1.14</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <!-- Localhost for testing -->
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <!-- Production API with HTTPS -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">amfserver.duckdns.org</domain>
        <domain includeSubdomains="true">api-nutricion.amfserver.duckdns.org</domain>
    </domain-config>

    <!-- Default: HTTPS only for all other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
